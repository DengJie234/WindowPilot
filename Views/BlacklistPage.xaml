<UserControl x:Class="WindowPilot.Views.BlacklistPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <CheckBox x:Name="WhitelistModeCheck" Content="启用白名单模式" VerticalAlignment="Center" Margin="0,0,18,0" Checked="WhitelistMode_Changed" Unchecked="WhitelistMode_Changed"/>
            <ComboBox x:Name="ItemTypeCombo" Width="140" Margin="0,0,8,0"/>
            <TextBox x:Name="ItemValueText" Width="260" Margin="0,0,8,0"/>
            <Button Content="加入黑名单" Click="AddBlacklist_Click" Padding="12,7" Margin="0,0,8,0"/>
            <Button Content="加入白名单" Click="AddWhitelist_Click" Padding="12,7" Margin="0,0,8,0"/>
            <Button Content="删除选中项" Click="DeleteSelected_Click" Padding="12,7"/>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="黑名单" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <DataGrid x:Name="BlacklistGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="130"/>
                        <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="*"/>
                        <DataGridTextColumn Header="启用" Binding="{Binding IsEnabled}" Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
            <DockPanel Grid.Column="1" Margin="12,0,0,0">
                <TextBlock DockPanel.Dock="Top" Text="白名单" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <DataGrid x:Name="WhitelistGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="130"/>
                        <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="*"/>
                        <DataGridTextColumn Header="启用" Binding="{Binding IsEnabled}" Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
