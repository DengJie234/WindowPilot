<Window x:Class="WindowPilot.Views.Dialogs.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="500"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Border Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="16"
                Padding="22">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="6" Opacity="0.18" Color="#111827"/>
            </Border.Effect>

            <StackPanel>
                <DockPanel>
                    <Border Width="42"
                            Height="42"
                            CornerRadius="12"
                            Background="{Binding AccentBackground}"
                            Margin="0,0,14,0">
                        <TextBlock Text="{Binding BadgeText}"
                                   Foreground="{Binding AccentForeground}"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" Foreground="{StaticResource PrimaryTextBrush}" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Message}" Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" Margin="0,6,0,0"/>
                    </StackPanel>
                </DockPanel>

                <Border Background="#F8FAFC"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="14"
                        Margin="0,18,0,18"
                        Visibility="{Binding HasDetailLines, Converter={StaticResource BoolToVisibility}}">
                    <ItemsControl ItemsSource="{Binding DetailLines}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,6"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{Binding CancelButtonText}" Style="{StaticResource GhostButton}" Click="Cancel_Click" Margin="0,0,8,0"/>
                    <Button Content="{Binding SecondaryButtonText}" Style="{StaticResource SecondaryButton}" Click="Secondary_Click" Margin="0,0,8,0" Visibility="{Binding HasSecondaryButton, Converter={StaticResource BoolToVisibility}}"/>
                    <Button Content="{Binding PrimaryButtonText}" Style="{Binding PrimaryButtonStyle}" Click="Primary_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
